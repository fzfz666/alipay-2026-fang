
# 2026 数字凑单神器 - 使用教程

这是一个帮大家快速计算如何凑齐 **2026** 的小工具。即使你有几百个数字，往里一粘，它也能瞬间告诉你哪两个数字能配对！

## 🛠 第一步：安装环境 (如果电脑没装过 Python)

这个工具需要一个极其轻量的小环境（Python）来帮你在本地保存数据，防止刷新网页数据丢失。

1. **下载**：点击链接下载 Python 安装包：[Python 官方下载 (Windows)](https://www.python.org/ftp/python/3.12.1/python-3.12.1-amd64.exe)
2. **安装 (最重要的一步！)**：
   * 双击运行安装包。
   * **🔴 必须勾选最底部的框： `Add python.exe to PATH`** （这一步不做后面会用不了！）
   * 然后点中间大大的 `Install Now`。
   * 等进度条跑完，点击 `Close`。

## 📂 第二步：准备文件

1. 在桌面上新建一个文件夹，名字随便起，比如叫 `凑单神器`。
2. 在这个文件夹里，新建两个文本文件（记事本）：
   * 第一个文件命名为：`server.py` (注意后缀是 .py 不是 .txt)
   * 第二个文件命名为：`index.html` (注意后缀是 .html)
3. 把群主给的代码分别复制进去，保存。

## 🚀 第三步：启动神器

1. 打开那个 `凑单神器` 文件夹。
2. 在文件夹的**空白处**，按住键盘上的 `Shift` 键，同时点击鼠标**右键**。
3. 在弹出的菜单里选择 **“在此处打开 Powershell 窗口”** (或者 “在终端中打开”)。
4. 在出来的黑底或者是蓝底窗口里，输入下面这行字，然后按回车：
   ```bash
   python server.py
   ```
5. 如果看到显示 `启动成功！请在浏览器打开: http://localhost:8000`，说明成功了！
   *(注意：这个黑窗口不要关，关了就不能保存数据了，把它最小化就行)*

## 💻 第四步：开始使用

1. 打开你的浏览器（推荐 Chrome 或 Edge）。
2. 在地址栏输入：`http://localhost:8000`
3. **开始操作**：
   * **左上角**：设置你固定的那个数字（默认 691）。
   * **模式切换**：
     * 🔒 **严格模式**：如果你必须包含一个 800 以上的大数，选这个。
     * 🔓 **自由模式**：如果你只是想随便找两个数凑单，选这个。
   * **输入数据**：
     * 直接复制 Excel 里的一列数字，或者是聊天记录里的数字。
     * 粘贴到中间那个大黑框里，按 **回车(Enter)**。
     * 系统会自动把数字分类，并告诉你结果。
   * **看结果**：
     * **左边绿色区域**：这就是已经凑成功的方案！
     * **右边橙色区域**：这是“急需”的数字。比如它显示 `需 1235`，那你就去群里喊：“谁有 1235！我有你的另一半！”

## ❓ 常见问题

* **Q: 为什么我双击 `index.html` 打开后不能保存数据？**
  * A: 必须通过 `python server.py` 启动，然后用 `localhost:8000` 访问才行，直接双击文件是存不了盘的（这是浏览器的安全限制）。
* **Q: 输入 `python server.py` 报错说找不到 python？**
  * A: 你安装的时候肯定没勾选 `Add to PATH`。卸载重装，记得勾选那个框！
